[build]
builder = "DOCKERFILE"
dockerfilePath = "codexa-backend/Dockerfile"

[deploy]
startCommand = "cd codexa-backend && sh start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
